import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Invoices',
};

const Page = () => {
  // console.log(ubaid)
  console.log(!231);
  return <div>Customers</div>;
};

export default Page;
